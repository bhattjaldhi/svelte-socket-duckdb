version: '3.8'

services:
  flask:
    build:
      context: ../flask-server
    ports:
      - "5000:5000"
    networks:
      - app-network

  svelte:
    build:
      context: ../svelte-app
    ports:
      - "80:80"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
